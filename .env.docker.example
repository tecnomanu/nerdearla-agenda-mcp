# === CONFIGURACIÓN DOCKER PARA NERDEARLA AGENDA MCP ===
# Copia este archivo como .env para personalizar la configuración

# === PUERTO ===
# Puerto donde se expondrá el servidor MCP
MCP_PORT=3000

# === AUTENTICACIÓN (OPCIONAL) ===
# Descomenta para habilitar autenticación Bearer
# MCP_BEARER=tu-token-secreto-aqui-2024

# === CORS ===
# Orígenes permitidos (separados por coma)
# Usa * para permitir todos los orígenes (no recomendado en producción)
ALLOWED_ORIGINS=*

# === CONFIGURACIÓN TÉCNICA ===
# Límite de tamaño del body de requests
MCP_BODY_LIMIT=1mb

# Nivel de logging
LOG_LEVEL=info

# === EJEMPLOS DE CONFIGURACIÓN ===

# Configuración de desarrollo (sin auth)
# MCP_PORT=3000
# ALLOWED_ORIGINS=*

# Configuración de producción (con auth)
# MCP_PORT=8080
# MCP_BEARER=nerdearla-super-secret-token-2024
# ALLOWED_ORIGINS=https://mi-dominio.com,https://n8n.mi-dominio.com

# Configuración para n8n local
# MCP_PORT=3000
# MCP_BEARER=n8n-local-token-123
# ALLOWED_ORIGINS=http://localhost:5678,http://localhost:3000
