# === CONFIGURACIÓN PRINCIPAL ===
# Transporte: 'http' para servidor HTTP, 'stdio' para STDIO
MCP_TRANSPORT=http

# Puerto donde se expondrá el servidor HTTP (solo aplica si MCP_TRANSPORT=http)
MCP_PORT=3000

# === CONFIGURACIÓN OPCIONAL ===
# Token Bearer para autenticación HTTP (opcional)
# MCP_BEARER=mi-token-secreto

# Orígenes permitidos para CORS (default: *)
# ALLOWED_ORIGINS=http://localhost:3000,https://mi-app.com

# Límite de tamaño del body para requests HTTP (default: 1mb)
# MCP_BODY_LIMIT=5mb
